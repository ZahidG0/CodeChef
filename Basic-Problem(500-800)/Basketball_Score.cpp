#include <iostream>
using namespace std;

int main() {

  // Replace the _ (underscores) with the correct value
  int x, y;
  cin >> x >> y;
  
  int res = (x * 3) + (y * 2);
  
  cout << res << endl;

}